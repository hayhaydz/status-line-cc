{
  "name": "statusline-hyz-cc",
  "version": "0.1.0",
  "private": false,
  "type": "module",
  "description": "Lightweight Claude Code CLI status line for GLM Coding Plan users",
  "scripts": {
    "build": "bun build src/main.ts --compile --outfile=build/statusline-hyz-cc",
    "install": "bun run build && mkdir -p ~/.claude/statusline-hyz-cc && cp build/statusline-hyz-cc ~/.claude/statusline-hyz-cc/",
    "dev": "bun run src/main.ts",
    "test": "bun test",
    "test:fixtures": "bun test test/fixtures.integration.test.ts",
    "test:scenarios": "bun run scripts/run-hook-scenarios.ts",
    "verify": "bun run build && bun run test:fixtures"
  },
  "keywords": [
    "claude-code",
    "statusline",
    "glm",
    "cli"
  ],
  "author": "hayhaydz",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^25.3.0",
    "bun-types": "^1.3.9"
  },
  "dependencies": {
    "dotenv": "^17.3.1"
  }
}
